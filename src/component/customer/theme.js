import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // Adjust the main color as per your preference
    },
    secondary: {
      main: '#f50057', // Adjust the secondary color as per your preference
    },
    background: {
      default: '#f0f0f0', // Adjust the default background color as per your preference
    },
  },
});

export default theme;
